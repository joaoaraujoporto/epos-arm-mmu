# INE5424-ARM-MMU

Pré-requisitos:
	- GNU Embedded Toolchain for Arm (No Ubuntu: $ sudo apt-get install gcc-arm-none-eabi)
	- GNU Make (No Ubuntu: $ sudo apt-get install make)

Para executar:
	$ make run
